node processing time Search 2 The second phase of the algorithm forms the strongly connected components of the graph. First, the algorithm reverses every edge in the graph. This guarantees that during the second search, we will always ﬁnd strongly connected components that do not have extra nodes. After reversing the edges, the example graph is as follows: After this, the algorithm goes through the list of nodes created by the ﬁrst search, in reverse order. If a node does not belong to a component, the algorithm creates a new component and starts a depth-ﬁrst search that adds all new nodes found during the search to the new component. In the example graph, the ﬁrst component begins at node 3: Note that since all edges are reversed, the component does not ”leak” to other parts in the graph.